{
  "sourceRoot": "src",
  "entryFile": "app",
  "compilerOptions": {
    "assets":["**/*.ejs", "**/*.ttf"],
    "tsConfigPath": "tsconfig.build.json",
    "plugins": [{
      "name": "@nestjs/graphql",
      "options": { "introspectComments": true }
    }, {
      "name": "@nestjs/swagger",
      "options": { "introspectComments": true }
    }]
  }
}
